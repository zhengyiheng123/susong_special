<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/material_white</item>
        <item name="colorPrimaryDark">@color/material_white</item>
        <item name="colorAccent">@color/theme_color</item>
        <item name="android:textColorSecondary">@color/material_grey_700</item>
    </style>
    <!--启动页-->


    <!--为 Theme 设置透明属性 -->
    <style name="start" parent="android:style/Theme.Black.NoTitleBar.Fullscreen" type="text/css">
        <item name="android:windowIsTranslucent">true</item>
    </style>


    <style name="CustomDialogTheme" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
        <!--   将dialog背景色置为透明   -->
        <item name="android:background">@android:color/transparent</item>
        <!--弹出动画-->
        <item name="android:windowEnterAnimation">@anim/dialog_enter_anim</item>
        <!--退出动画-->
        <item name="android:windowExitAnimation">@anim/dialog_exit_anim</item>
    </style>

    <!--dialog动画-->
    <style name="dialogWindowAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_anim</item>
    </style>
    <!--带输入框-->
    <style name="inputDialog" parent="@android:style/Theme.Holo.Light.Dialog">
        <item name="android:windowFrame">@color/touming</item><!-- 边框 -->
        <item name="android:windowIsFloating">true</item><!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item><!-- 半透明 -->
        <item name="android:windowNoTitle">true</item><!-- 无标题 -->
        <item name="android:windowBackground">@color/touming</item><!-- 自己想要的背景 -->
        <item name="android:backgroundDimEnabled">true</item><!-- 模糊 -->
        <item name="android:windowSoftInputMode">stateAlwaysVisible</item><!--显示软件盘-->
    </style>
</resources>
